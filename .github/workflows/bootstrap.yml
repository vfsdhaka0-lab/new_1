name: Bootstrap Android Flutter Project
on: [push, workflow_dispatch]
jobs:
  bootstrap:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: 'stable'
      - name: Create Flutter Project
        run: |
          find . -maxdepth 1 ! -name '.github' ! -name '.' -exec rm -rf {} +
          flutter create --org com.aman.ai --project-name new_1 --platforms android,web .
          git config --global user.name "github-actions[bot]"
          git config --global user.email "actions@github.com"
          git add .
          git commit -m "Generate Flutter project via Aman IDE [skip ci]"
          git push
